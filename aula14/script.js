/* let idade = prompt("Digite sua idade: ");

if (idade >= 18 && idade <= 120) {
    alert("Acesso liberado!");
}else if (idade < 0 || idade > 120) {
    alert("Idade inválida!");
}else {
    alert("Acesso negado!");
} */

/* ehHomem = prompt("Você é homem? (sim/não)").toLowerCase();
let temIngresso = prompt("Você tem ingresso? (sim/não)").toLowerCase();

if (ehHomem == "não" || temIngresso == "sim") {
    alert("Acesso liberado!");
}else{
    alert("Acesso negado!");
} */

/* let opcao = prompt("Escolha uma opção: \n 1 - Cadastrar \n 2 - Listar \n 3 - Excluir").toLowerCase();

switch (opcao) {
    case "1":
        alert("Você escolheu cadastrar!");
        break;
    case "2":
        alert("Você escolheu listar!");
        break;
    case "3":
        alert("Você escolheu excluir!");
        break;
    default:
        alert("Opção inválida!");
        break;
} */

/* for (let i = 0; i < 5; i++) {

    alert("Número: " + i);
} */

/* while (true) {
    let numero = prompt("Digite um número (ou 'sair' para encerrar): ");

    if (numero.toLowerCase() === "sair") {
        break;
    }

    numero = parseInt(numero);

    if (isNaN(numero)) {
        alert("Por favor, digite um número válido.");
    } else {
        alert("Você digitou o número: " + numero);
    }
} */